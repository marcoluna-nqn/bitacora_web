name: bitacora_web
description: "BitÃƒÂ¡cora web: hoja tipo Excel con experiencia tipo Apple."
publish_to: "none"
version: 1.3.1+11

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Utilidades
  intl: 0.20.2
  collection: ^1.18.0
  image: ^4.2.0       # Para convertir HEIC/WEBP a JPEG en exportaciÃƒÂ³n

  # NavegaciÃƒÂ³n
  go_router: ^16.3.0

  # Estado / preferencias
  shared_preferences: ^2.3.2

  # Persistencia
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Syncfusion
  syncfusion_flutter_core: ^31.2.4
  syncfusion_flutter_datagrid: ^31.2.4
  syncfusion_flutter_xlsio: ^31.2.4
  pdf: ^3.11.3

  # Archivos / exportaciÃƒÂ³n
  file_selector: ^1.0.4
  file_saver: ^0.3.1
  csv: ^6.0.0
  path: ^1.9.1
  path_provider: ^2.1.4
  archive: ^4.0.7

  # UX
  flutter_animate: ^4.5.2
  animations: ^2.0.11
  lottie: ^3.1.2
  url_launcher: ^6.3.2
  video_player: ^2.9.2
  record: ^6.2.0
  audioplayers: ^6.1.0

  # Compartir / correo
  share_plus: ^12.0.1
  flutter_email_sender: ^8.0.0

  # Voz a texto
  speech_to_text: ^7.3.0
  permission_handler: ^11.3.1

  # GPS / ubicaciÃƒÂ³n
  geolocator: ^12.0.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.0

  # DiagnÃƒÂ³stico (opcional)
  sentry_flutter: ^9.7.0
  package_info_plus: ^9.0.0

  # HTTP / APIs
  http: ^1.5.0
  dio: ^5.9.0
  dio_web_adapter: ^2.1.1
  web: ^1.1.1

  # Firebase (dejados solo si usÃƒÂ¡s Firestore en otra parte)
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.1.0
  image_picker: ^1.2.1
  google_sign_in: ^7.2.0
  local_auth: ^2.3.0
  local_auth_android: ^1.0.53
  local_auth_darwin: ^1.6.1
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.9
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/version.json
    - assets/config.json
    - assets/config.example.json
    - assets/sfx/
    # - assets/video/gridnote_cells_loop.mp4

  # Fuentes locales: evitan que Flutter Web (CanvasKit) baje Roboto desde
  # fonts.gstatic.com. Necesario para redes corporativas que bloquean CDNs.
  fonts:
    - family: Roboto
      fonts:
        - asset: assets/fonts/roboto/Roboto-Regular.ttf
        - asset: assets/fonts/roboto/Roboto-Medium.ttf
          weight: 500
        - asset: assets/fonts/roboto/Roboto-Bold.ttf
          weight: 700
        - asset: assets/fonts/roboto/Roboto-Black.ttf
          weight: 900

